# Copy to revisit later

Parked on request (18 September 2026): wording is not the priority right now.
Nothing below blocks code; each is a sentence to review when copy is picked up.

## Permanence (pump `admin_cto`)
Already corrected in code (patches 0010-0011); review tone, not facts:
- `indexer/enroll_page.py`, `indexer/launch_page.py`: split-is-final sentences
  now say pump's admin can still reset via `admin_cto`.
- `indexer/site.py` enrollment panel, `indexer/flywheel_page.py` lede,
  `indexer/buildlog_page.py`, `indexer/splitter_page.py`, `README.md`.
- `indexer/launch_page.py` badge "ONE CHANGE, EVER" and headline were kept /
  lightly changed; decide on final phrasing.
- New page state "This coin's creator was replaced" (`site.py`) and its intake
  reply (`intake.py`): first-draft wording.

Not yet audited for the same overclaim: anything outside this repo (the live
site's hero and gate page, social posts, the Phase 5 materials in project
knowledge).

## `/launchlab` page (`indexer/launchlab_page.py`)
First-draft copy, written only to state what the record shows. Needs a pass:
lede, devnet banner, the four crank descriptions, "What can still change",
column headings. Numbers on the page come from `state/launchlab/devnet.json`
and are not copy.

## Not linked anywhere yet
`/launchlab` has a route (`vercel.json`) and is generated by `refresh`, but no
other page links to it. Decide where it belongs (buildlog, phases, nav).

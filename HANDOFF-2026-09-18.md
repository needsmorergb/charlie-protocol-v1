# Handoff -- 18 September 2026

Branch `claude/custom-pairs`, 14 commits on `main` (`f6e922e`). Tests: Python
1,037 pass (`python3 -m unittest discover -s tests`), Rust 33 pass in both
builds (`cargo test --lib --tests` with and without `--features devnet`).

## Where things stand

**Mainnet go-live of the SOL-paired LaunchLab rail: ready, not yet run.**
Adam runs it himself (it spends real SOL and needs his key; Claude does not
sign mainnet transfers). One command, from the repo root:

    CHARLIE_WALLET=<buyback wallet> ./tools/launchlab_mainnet_go.sh

needs `charlie_launchlab_mainnet.so` and `charlie_launchlab-program-keypair.json`
(delivered in the chat outputs, not committed) and ~3.3 SOL. It checks,
deploys, verifies the deployed bytes, simulates and sends the platform setup,
and prints the lookup table for the keeper. Full runbook: LAUNCHLAB-MAINNET.md.

**First thing next session:** ask whether it ran; if so, verify on mainnet
(read-only): upgrade authority = Adam's wallet; `ll_platform` charlie_pool =
the buyback wallet; Raydium platform config (PDA `["platform_config",
ll_signer]` under LaunchLab) fee 2,500, creator fee 5,000, scales 1,000,000/0/0,
cpswap config at offset 688 = `EUZHCdd8...`; quote_cfg(WSOL) min_crank; the
lookup table's contents. Then put the table address in
`indexer/launchlab_keeper.py` CLUSTERS['mainnet']['lookup_table'].

## Decisions made (Adam, 18 Sept)

- Solo operator: no multisig. His wallet = upgrade authority and platform admin.
- SOL pairing live now; USDC "shortly" (next work item); stocks after, behind legal.
- Charlie leg (0.25% platform fee + 25% of LP fees) pays a dedicated wallet he
  uses to buy and burn $CHARLIE. Fixed at setup; changing it needs an upgrade.
- Post-graduation pools: Raydium CPMM config index 8
  `EUZHCdd8H7nueb2wLpxUqyuemdbe8TUxfCWcxVfARvgw` (0.25% trade, 0.55% creator).
- Copy is parked: COPY-TODO.md lists what to review; don't spend time on wording.

## Mainnet facts (read 18 Sept, all read-only)

- Program id `GM6ET1LceNLkHUWefD79eeeJzFRk8yQwfnR7pdYP5d6e`, build SHA-256
  `f3182985...51ef`, 221,384 bytes.
- LaunchLab platform creation is permissionless (2,437 exist); the rail's
  platform params simulate OK; SOL global config `6s1xP3hp...aHuX` exists;
  1B/793.1M curve simulates OK at 85 and 30 SOL raises.
- All 21 CPMM configs have creatorFeeShareRate 0; no creator_fee_share accounts.

## What was built this session

- LaunchLab rail steps 1-4, 6, 7 (LAUNCHLAB-RAIL.md sec.8): all four cranks run
  on devnet with every lamport reconciled; keeper-rent fix; LaunchLab rejects a
  creator LP slice (LP 100% to platform NFT, split 25/75 by crank_lp).
- Deployed CPMM is ahead of the public source: collect needs `amm_config` and
  `["creator_fee_share", creator, amm_config]` (SDK 0.2.70-alpha).
- `indexer/launchlab_keeper.py` (stdlib), validated against live devnet state
  (fixture `tests/fixtures/launchlab_devnet_state.json`); v0 messages via
  `indexer.message.compile_v0`.
- Tools: `launchlab_mainnet_setup.py`, `launchlab_launch.py` (CLI launch; no
  site page yet), `launchlab_devnet.py` (records `/launchlab`),
  `launchlab_mainnet_go.sh`.
- Indexer: `creator_replaced` result when pump's `admin_cto` moves an enrolled
  coin's creator (was misreported as "never enrolled"); permanence copy no
  longer says nobody can change a split.
- `/launchlab` page (devnet runs; generated, routed, not linked anywhere).

## Open, in rough order

1. Verify the mainnet go-live (above); run the keeper on a schedule.
2. USDC pairing on the rail: check a USDC LaunchLab global config exists on
   mainnet; design the non-SOL legs (SOL-burn leg and Charlie leg need a swap
   or a USDC destination) -- bring choices to Adam; build; test on a mainnet fork.
3. Own-burn buy has no on-chain price bound (sandwich risk; why cranks are
   admin-only). Needed before cranks go permissionless.
4. Separate keeper role from admin (so the admin key can stay offline).
5. Launch page on the site (currently CLI only).
6. Ops/Charlie destinations must be pre-funded (rent-exempt) or a crank reverts.
7. Deploy the site/indexer changes: `python tools/shared_sync.py --copy-to
   ../charlie-protocol-site`, then deploy.
8. Pump rail leftovers: read pump `Global.whitelisted_quote_mints`,
   `FeeConfig.exotic_flat_fees`; 200-coin sample to flip
   `pump.QUOTE_FIELDS_SAMPLED`.
9. Stocks (Token-2022 quote, step 5): needs a Raydium config for the quote and
   a local mainnet fork to test; legal gate for US persons.

## Devnet leftovers

Program `4WgYTmPM...uJWgX` (upgrade authority = browser-held deployer
`Fdq7GiMh...AzAq9V`, key in explorer.solana.com localStorage
`charlie_devnet_deployer`), graduated test coin `BuX4dN...`, lookup table
`6dGcRtaw...8xrMovx`.

## Sandbox notes (if rebuilding)

Agave 2.2.20 at /tmp/solana-release/bin, rustc/cargo 1.85 via apt, shims at
/tmp/shim, `cargo-build-sbf --tools-version v1.51 [--features devnet]`. The
sandbox cannot reach Solana RPC or github.com push; use the browser for chain
reads. Large binaries reach the browser as verified base64 chunks, or as a
copy/insert patch against the deployed bytes (much smaller).
